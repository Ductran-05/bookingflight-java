spring.datasource.url=${spring.datasource.url}
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}
spring.datasource.driver-class-name=${spring.datasource.driver-class-name}

spring.jpa.database-platform=${spring.jpa.database-platform}
spring.jpa.hibernate.ddl-auto=${spring.jpa.hibernate.ddl-auto}
# spring.jpa.show-sql=true

projectjava.jwt.base64-secret=${projectjava.jwt.base64-secret}
projectjava.jwt.access-token-validity-in-seconds=${projectjava.jwt.access-token-validity-in-seconds}
projectjava.jwt.refresh-token-validity-in-seconds=${projectjava.jwt.refresh-token-validity-in-seconds}

spring.mail.host=${spring.mail.host}
spring.mail.port=${spring.mail.port}
spring.mail.username=${spring.mail.username}
spring.mail.password=${spring.mail.password}
spring.mail.properties.mail.smtp.auth=${spring.mail.properties.mail.smtp.auth}
spring.mail.properties.mail.smtp.starttls.enable=${spring.mail.properties.mail.smtp.starttls.enable}

spring.servlet.multipart.max-file-size=${spring.servlet.multipart.max-file-size}
spring.servlet.multipart.max-request-size=${spring.servlet.multipart.max-request-size}
spring.servlet.multipart.enabled=${spring.servlet.multipart.enabled}
spring.servlet.multipart.resolve-lazily=${spring.servlet.multipart.resolve-lazily}

cloudinary.cloud-name=${cloudinary.cloud-name}
cloudinary.api-key=${cloudinary.api-key}
cloudinary.api-secret=${cloudinary.api-secret}

google.genai.api-key=${google.genai.api-key}

spring.security.oauth2.client.registration.google.client-id=${spring.security.oauth2.client.registration.google.client-id}
spring.security.oauth2.client.registration.google.client-secret=${spring.security.oauth2.client.registration.google.client-secret}
spring.security.oauth2.client.registration.google.scope=${spring.security.oauth2.client.registration.google.scope}
spring.security.oauth2.client.registration.google.redirect-uri=${spring.security.oauth2.client.registration.google.redirect-uri}
spring.security.oauth2.client.provider.google.authorization-uri=${spring.security.oauth2.client.provider.google.authorization-uri}
spring.security.oauth2.client.provider.google.token-uri=${spring.security.oauth2.client.provider.google.token-uri}
spring.security.oauth2.client.provider.google.user-info-uri=${spring.security.oauth2.client.provider.google.user-info-uri}
spring.security.oauth2.client.provider.google.user-name-attribute=${spring.security.oauth2.client.provider.google.user-name-attribute}